---
# Report Role Defaults
# Generate compliance audit reports

# Required variables (override in job template)
tenant_namespace: "compliance"
report_period_start: ""  # ISO date: 2026-01-01
report_period_end: ""    # ISO date: 2026-03-31

# Report configuration
report_format: "html"  # html | json | csv
report_template: "standard"  # standard | executive | detailed

# Compliance thresholds (match your Grafana/alerting thresholds)
compliance_threshold_compliant: 70  # Score >= this is "Compliant" (green)
compliance_threshold_warning: 50    # Score >= this but < compliant is "Warning" (yellow)
# Score < warning threshold is "Critical" (red)

# Include sections
include_evidence: true
include_remediation_history: true
include_trend_analysis: true

# Output configuration
report_output_dir: "/tmp/compliance-reports"
report_filename_prefix: "compliance-report"

# Storage
push_to_storage: true
storage_backend: "pvc"  # pvc | s3
reports_storage_class: ""  # Leave empty for cluster default, or specify e.g. "ocs-external-storagecluster-cephfs"

# Notification
send_notification: false
notification_recipients: []
